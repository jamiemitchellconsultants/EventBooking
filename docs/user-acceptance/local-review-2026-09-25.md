# Local Docker review — 25 September 2026

[← Acceptance pack](README.md) · [Home-lab review](live-review-2026-09-25-after-ui-fixes.md)

**Environment:** [EventBooking at localhost:5002](http://localhost:5002/) with the local Keycloak, API, PostgreSQL, and Mailpit containers. The operator confirmed deployment and data reseed before the results below. The API readiness check returned HTTP 200. The acceptance branch includes `origin/main` at `da20c96`; the exact commit baked into the running containers was not exposed in the UI.

**Method:** browser walkthrough with the documented local demo accounts and the latest invitation for the synthetic attendee Nia New. Private invitation and management tokens were kept out of this record. The only booking changed in this run was Nia's: she first booked 4 October, cancelled to request a fresh invitation, then booked 7 October. She ended with one active booking on 7 October; event capacity changed with that booking.

| Case | Local result | Evidence and limit |
| --- | --- | --- |
| UAT-00 | Pass locally | The signed-out landing page renders without a visible error strip; public Help displays the attendee guide. Admin, Coordinator, Manager, and Appointment staff demo users signed in and reached role-specific homes. |
| ADM-01 | Fail for signed-in Help; access boundary passes | Admin sees **Your work** and **Administration** links. A direct visit to `/attendees` says the attendee list needs a Coordinator profile and shows no attendee records. Signed-in Help instead says the Admin account has no assigned role. |
| ADM-02–05 | Partially checked | Locations, Appointment types, Attendee groups, System settings, Staff access, Event operations, and Audit search loaded. The location and group edit forms, invitation settings, staff scope list, and event-cancellation confirmation were inspected without saving or cancelling. No write or audit-result assertion was run. |
| MGR-01 | Fail for proposal classification | The Fitness Manager home shows the expected scope and links. **Open proposals** includes windows already accepted by all listed types, such as the 1 October window at 2 of 2, while the same window appears under **Confirmed events**. The two lists are not behaving as their headings describe. |
| MGR-02–04 | Partially checked | The proposal form fixes the caller's type, disables a type with no Manager, and shows the local end time. The Fitness appointment workspace shows a type-scoped roster. Proposal acceptance, capacity changes, and a roster export were not run. |
| COO-01 | Fail for required-type display | Dashboards show Awaiting availability, No response, and Events; Attendees has the expected filters and controls. Several booked attendee rows have empty **Required types** cells. Editing one such row shows its group and FIT, IND, and MED requirement chips. |
| COO-02–07 | Partially checked | The New attendee form derives a required type from its group; Import shows the CSV upload control; an invite form reports 0 eligible events against a need for 3. The No response dashboard offers **Re-invite now**. Readiness identifies outstanding and recoverable types. A new attendee, CSV import, recovery, resend, and event cancellation were not executed. |
| ATT-01 | Pass locally | Nia's fresh invitation offered three future windows with location, address, local time, and time zone. Selection enabled **Confirm this time**; confirmation displayed the chosen window and a management link. A confirmation email arrived, the Coordinator view showed one booking, and reusing the invitation displayed **This link has expired.** |
| ATT-02 | Pass for rescheduling | **Manage your booking** showed Nia's appointment. **Cancel and choose a new time** cancelled it and sent a fresh invitation. Choosing a different window left one active booking on 7 October in the Coordinator view. The separate **Cancel booking** scenario was not run. |
| ATT-03 | Partially checked | Public Help works locally. The phone-sized booking view, accessibility checks, and invalid-link cases were not repeated after this reseed. |
| APS-01 | Fail for signed-in Help; roster passes | Appointment staff sees only the Medical workspace. Its event selector groups windows by Location; the seeded roster shows Expected, Checked in, No-show, and Completed rows with actions appropriate to their state. Signed-in Help says the account has no assigned role. The CSV was not downloaded. |
| APS-02–03 | Not run | The relevant seeded events are in the future, so date-gated check-in and No-show transitions were not performed. |

Two further observations need product or fixture investigation before the corresponding expectations can be marked as passing: the seeded 4 October event shows five active bookings while Fitness, Induction, and Medical each display 12 of 12 places remaining; signed-in Help gives a no-role message even though the same session's home page displays the role. A newly made Induction booking on 7 October did reduce that type's remaining places from 12 to 11, so the earlier capacity mismatch may belong to the seed data. These results are specific to the local Docker deployment; the home-lab site needs its own signed-in and valid-link run.
