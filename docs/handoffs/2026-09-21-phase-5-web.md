<!-- A handoff prompt: paste the whole of this file into a fresh agent. It is written to be
     self-sufficient for an agent on a different machine, with no access to the scratch
     prototype Phases 0-2 were built in. Provenance: written 21 September 2026, after Phase 4
     was authored and reviewed twice. -->

Work on the EventBooking repository: https://github.com/jamiemitchellconsultants/EventBooking

Clone it fresh, then read docs/detailed-implementations/HANDOVER.md before anything else. It is
written for an agent with no context and tells you the assignment, the governing inputs and their
precedence, the standing rules that have already bitten, and how far the work has got. Follow it.

Your job: author Phase 5, master Tasks 24, 25, 26 and 27 — the web work. Their specs are in
docs/superpowers/plans/2026-09-19-eventbooking-implementation.md under "Phase 5 — Web". The
component contracts are docs/design/03a-design-system-and-ia.md, the screens and their states are
docs/design/03b-screens-and-flows.md, and the API the client consumes is
docs/design/05-api-design.md as Phase 4 put it on the wire.

BEFORE YOU START, check that pull request #27 has merged. If it is still open, stop and say so
rather than working around it. Phase 5 is a client for what Phase 4 built: the endpoint catalogue,
the RFC 9457 problem catalogue, the signed cursor and page contract, the one event-time
representation, and `_links` generated from the caller's capabilities. Without them you would be
writing a client for an API that does not exist yet, and every contract test would be asserting
against a shape you invented.

THE METHOD IS SETTLED, AND IT IS NOT QUITE PHASE 4's.

Phase 5 stays hand-authored — complete code and complete tests written straight into each
document, no prototype, because the executing model compiles and test-drives them itself. Section
6a's loop is your method, and both of its mechanical sweeps still run per task with their match
counts printed.

One thing changes, and it is the reason this phase needed a decision at all. **The Playwright and
axe-core project the master plan puts in Task 27 moves to Task 24.** Task 24 creates
`tests/EventBooking.Web.E2E/` alongside the design-system components, every later task adds its
own routes to it, and the phase gate is zero axe violations on every route at mobile and desktop
widths. The workflow change that runs it — the axe job in `.github/workflows/dotnet-build.yml` —
moves to Task 24 with it.

The reason: section 6a's sweeps do not reach this phase. The gap sweep still works, but the async
sweep matches little in Razor markup, and neither can tell a working page from a plausible one.
That is the failure mode the handover's Phase 5 checkpoint names, and a rendered page that axe
walks is the only cheap substitute for the compiler Phases 0-2 had. Do not leave it to Task 27,
where three tasks of components would already be written before anything rendered them.

Record the move as a settlement in the handover's section 8, with that reason, and answer section
11's Phase 5 checkpoint with it. Do not reopen the hand-authoring decision itself.

THE PROTOTYPE IS NOT ON YOUR MACHINE. Read this before you go looking for it.

Phases 0-2 were built in a scratch checkout under /private/tmp on one particular machine. The
handover references it, section 6a's step 4 tells you to read it, and it does not exist for you.
Do not try to recreate it and do not guess at type shapes because you cannot see it.

You do not need it, and for this phase you will use the reader constantly: Tasks 25, 26 and 27
modify ported pages rather than create new ones, and you cannot modify a file you have not read.
Save this script as at-task-11.py in the repository root and run it. It is tested — these exact
invocations were verified working:

```python
#!/usr/bin/env python3
"""Print a file's content as it stands at the end of Phase 2 (Task 11).

Usage: python3 at-task-11.py src/EventBooking.Web/Pages/Attendees.razor
The plan's edit volumes carry every file's complete after-side; the last volume
in task order that names the path holds its current state.
"""
import glob, re, sys

path = sys.argv[1]
marker = f"## after — {path} — "
newest = None
for vol in sorted(glob.glob("docs/detailed-implementations/phase-[0-2]*.md")):
    text = open(vol).read()
    if marker in text:
        newest = (vol, text)
if newest is None:
    sys.exit(f"No after-side for {path}. It may be created later than Task 11, or deleted.")

vol, text = newest
parts = []
for m in re.finditer(re.escape(marker) + r"(\d+)/(\d+)\n\n<!--.*?-->\n\n`{5}\w*\n(.*?)\n`{5}", text, re.S):
    parts.append((int(m.group(1)), m.group(3)))
print(f"# from {vol} ({len(parts)} part(s))", file=sys.stderr)
for _, body in sorted(parts):
    print(body)
```

Three caveats on it. It only covers Tasks 1-11, because Phases 3, 4 and 5 are hand-authored and
have no edit volumes — for an Application or Api type, read the phase-3*.md or phase-4*.md
document that creates it. A file created after Task 11 or deleted before it returns nothing, which
is information rather than an error. And "returns nothing" is the answer to several questions in
this phase, so read the next section before you conclude a file is missing.

THREE TRAPS THE MASTER PLAN WILL WALK YOU INTO. Each was checked against the volumes while this
prompt was written.

* **Task 26 names `Pages/Negotiate.razor` "(was the slots page)". No such file exists, and the
  parenthetical is wrong twice over.** Phase 0's vocabulary rename already did this work: the
  negotiation page is `Pages/EventNegotiation.razor` and the operations page is
  `Pages/EventOperations.razor`. `Slots.razor` and `ConfirmedSlots.razor` return nothing from the
  reader because they no longer exist. The same applies to `Candidates.razor`, which is
  `Pages/Attendees.razor`, and to `CandidateLayout.razor`, which is `Layout/AttendeeLayout.razor`.
  Write the real names into your Files lists, and check every path the master plan gives you with
  the reader before you copy it.
* **Task 27 says "Rewrite: role guides in `src/EventBooking.Web/wwwroot/help/`". There are no role
  guides to rewrite.** No file under `wwwroot/help/` exists at Task 11; what exists is
  `Pages/Help.razor`. Task 27 creates the guides. A Files list that says Modify: where the file
  does not exist is the failure Phase 3's review found seven times.
* **Task 24 deletes branded assets, and they are real files with real names.** The
  `wwwroot/fonts/mylius-Modern-*.woff2` family, `wwwroot/speedmarque.png`, and the branded parts of
  `wwwroot/css/app.css` and `wwwroot/css/fonts.css`. Name each one in the Files list rather than
  writing "JointBooking's branded tokens, fonts, logos and images", which is what the master plan
  says and is not a file list.

WHAT PHASE 4 GIVES YOU, AND WHAT YOU MUST NOT RE-DERIVE.

* **Errors are rendered from the slug, never from the message.** Task 21's problem catalogue is the
  closed set: every failure arrives as `application/problem+json` with a `type` from that
  catalogue. A screen that switches on prose is a screen that breaks when the prose changes.
* **Controls are enabled from `_links`, not from a role check in the client.** Task 21's link
  builder emits only the affordances the caller holds, which is what lets the web front end avoid
  restating the authorization matrix. Do not reimplement the matrix in Razor.
* **Times come from the API's one event-time representation.** Task 21 returns `date`, `startTime`,
  `durationMinutes`, `startLocal`, `endLocal`, `startUtc`, `endUtc`, `timeZoneId` and
  `zoneAbbreviation`. The EventTime component renders them; it does not recompute an end time or
  convert a zone.
* **Lists are `{ items, nextCursor }` with `?cursor=&limit=`, and there is no offset anywhere.**
  Task 22b's bounded lists return every row with a null cursor, deliberately; the unbounded ones
  page. Both arrive in the same envelope, so one DataTable reads both.
* **A missing or malformed staff number is 403, not 401, everywhere except `GET /api/me`**
  (contradiction #3). The client must not treat it as a sign-in prompt.
* **Create endpoints take an `Idempotency-Key`.** A form that can be double-submitted should send
  one rather than relying on the button.
* **Web never references server assemblies.** Client DTOs are duplicated in the Web project and
  covered by contract tests against the OpenAPI document Task 22b serves. That is the master plan's
  rule and it is not negotiable for convenience.

State you can rely on, so you do not re-derive it:

* Phases 0, 1 and 2 are prototype-verified, replayed and merged. The last measured checkpoint is
  Task 11: 1570 tests — Domain 360, Application 421, Infrastructure 206, API 232, MCP 35, Web 241,
  SeedData 75.
* Phase 3 (Tasks 12-20) and Phase 4 (Tasks 21-23) are written, reviewed and merged, and neither has
  ever been built or tested. Section 7 explains why they carry no test figures and must not be
  given any. Yours will not either. Say so in each document's own Step 4.
* **Task 21 deliberately lowers the suite count before Tasks 22b and 23 raise it** (settlement
  #19): Phase 3 left the Api and Mcp projects uncompilable, and Task 21 restores the build by
  deleting the orphaned routes, tools and the ported suites that drove them. If you are tempted to
  reconcile a Web figure against Task 11's 1570, do not — the comparison stopped being meaningful
  at Task 12.
* You are creating the phase overview too — phase-5-web.md, modelled on phase-4-api-and-mcp.md.
  Name the task documents phase-5a-, phase-5b-, phase-5c- and phase-5d-, continuing the lettering.
  Task 27 carries the phase's pull-request gate.

Standing rules worth repeating because each has cost a cycle:

* If a domain concept changes, edit docs/ontology.ttl, run node scripts/build-ontology.mjs, and
  commit both files together.
* Lint the plan directory explicitly before staging — the recipe is in the handover's section 9.
  The checker only sees tracked Markdown.
* In plan prose, backtick a PascalCase word only if the ontology defines it. Component, class, test
  and file names go in plain text or inside fenced code blocks. This phase invents a lot of
  component names, so this rule will bite more here than it did in Phase 4.
* Stage explicit paths. Never git add -A.
* Never push to main, never merge. One commit per task on the phase branch.
* The ai-fingerprint check goes red once on every push and that red is not yours to fix. Wait until
  the pull request reports the head you just pushed, then update the body.

Finish each task with its own commit and push on the phase branch. Do not open a pull request
without asking me first — and when you do, Task 27's is decision-bearing (D10, the neutral theme),
so it carries the narrative-required label and the three narrative headings.
